CREATE TABLE IF NOT EXISTS import (
    path VARCHAR,
    title VARCHAR,
    albumartist VARCHAR,
    artist VARCHAR,
    composer VARCHAR,
    genre VARCHAR,
    artistsort VARCHAR,
    album VARCHAR,
    bpm INTEGER,
    length INTEGER,
    year INTEGER
);